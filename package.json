{
  "name": "kakeibo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "api:restore": "dotnet restore Kakeibo.slnx",
    "api:build": "dotnet build Kakeibo.slnx --configuration Release",
    "api:format:check": "dotnet format Kakeibo.slnx --verify-no-changes",
    "api:test": "dotnet test tests/Kakeibo.Tests/ --configuration Release",

    "email:install": "cd src/Kakeibo.Email && bun install",
    "email:typecheck": "cd src/Kakeibo.Email && bun run typecheck",
    "email:lint": "cd src/Kakeibo.Email && bun run lint",
    "email:format": "cd src/Kakeibo.Email && bun run format",
    "email:format:check": "cd src/Kakeibo.Email && bun run format:check",
    "email:test": "cd src/Kakeibo.Email && bun run test",

    "app:install": "cd src/Kakeibo.App && bun install",
    "app:dev": "cd src/Kakeibo.App && bun run dev",
    "app:build": "cd src/Kakeibo.App && bun run build",
    "app:typecheck": "cd src/Kakeibo.App && bun run typecheck",
    "app:lint": "cd src/Kakeibo.App && bun run lint",
    "app:lint:check": "cd src/Kakeibo.App && bun run lint:check",
    "app:format": "cd src/Kakeibo.App && bun run format",
    "app:format:check": "cd src/Kakeibo.App && bun run format:check",
    "app:test:unit": "cd src/Kakeibo.App && bun run test:unit",
    "app:test:e2e": "cd src/Kakeibo.App && bun run test:e2e",

    "docker:build:api": "docker build -f src/Kakeibo.Api/Dockerfile -t kakeibo-api .",
    "docker:build:email": "docker build -f src/Kakeibo.Email/Dockerfile -t kakeibo-email ./src/Kakeibo.Email",
    "docker:build:app": "docker build -f src/Kakeibo.App/Dockerfile -t kakeibo-app ./src/Kakeibo.App",
    "docker:build:all": "bun run docker:build:api && bun run docker:build:email && bun run docker:build:app"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.4.2",
    "@commitlint/config-conventional": "^20.4.2",
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/commit-analyzer": "^13.0.1",
    "@semantic-release/exec": "^7.1.0",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/github": "^12.0.6",
    "@semantic-release/npm": "^13.1.4",
    "@semantic-release/release-notes-generator": "^14.1.0",
    "conventional-changelog-conventionalcommits": "^9.1.0",
    "lefthook": "^2.1.1",
    "semantic-release": "^25.0.3"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/rafitajaen/kakeibo.git"
  }
}
